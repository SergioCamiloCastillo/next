{
  "routes": [
    {
      "method": "GET",
      "path": "/precios",
      "handler": "precio.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/precios/count",
      "handler": "precio.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/precios/:id",
      "handler": "precio.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/precios",
      "handler": "precio.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/precios/:id",
      "handler": "precio.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/precios/:id",
      "handler": "precio.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
